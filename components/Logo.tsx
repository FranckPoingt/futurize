import React from 'react';
import styles from '../styles/Logo.module.css';

const Logo = () => {
    return (
        <div>
            <span className={styles.logo}>FUTURIZE.IO</span>
        </div>
    )
}

export default Logo
